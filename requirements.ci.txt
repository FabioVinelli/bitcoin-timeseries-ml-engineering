# Minimal CPU-safe dependencies for CI
# This is a reduced set that avoids heavy native builds (TA-Lib, GARCH, etc.)
# Full research environment uses requirements.txt

numpy<2
pandas
scikit-learn
pyyaml
joblib

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
